{
  "name": "angular-flatpickr-example",
  "private": true,
  "version": "0.0.0",
  "description": "An example project for angular-flatpickr",
  "repository": "https://github.com/archsaber/angular-flatpickr-example",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
   }
}
